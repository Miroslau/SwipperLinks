import {StyleSheet} from 'react-native';

export const TitleStyled = StyleSheet.create({
  container: {
    backgroundColor: 'red',
    borderRadius: 5,
    padding: 15,
  },
  title: {
    textAlign: 'center',
    fontSize: 25,
    color: 'white',
    fontWeight: 'bold',
  },
});
